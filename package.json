{
  "name": "pathfinder-dispatch",
  "version": "1.0.0",
  "private": true,
  "description": "A browser game that teaches graph pathfinding by making the trade-offs visible. The game itself has no build step and no dependencies; this file exists only to run the test suite.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
